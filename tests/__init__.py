"""tests/ — search-bot unit tests."""

import sys
import os

# Ensure src/ is on the path even when tests/ is run standalone
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))